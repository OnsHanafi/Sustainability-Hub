{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Contenu_reponse</th>
                <td>{{ reponse.contenuReponse }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ reponse.titre }}</td>
            </tr>
            <tr>
                <th>Email_reponse</th>
                <td>{{ reponse.emailReponse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}">back to list</a>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
